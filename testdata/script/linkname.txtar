! exec unparam .
cmp stdout stdout.golden

-- go.mod --
module testdata.tld/foo
-- stdout.golden --
foo.go:7:18: regularFunc - f is unused
-- foo.go --
package foo

import _ "unsafe"

var DoWork func()

func regularFunc(f int) { DoWork() }

//go:linkname linknamed other/pkg.linknamed
func linknamed(f int) { DoWork() }
